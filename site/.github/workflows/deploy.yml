name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Set basePath
        id: set-basepath
        run: |
          # Repository adını al (örn: efecanzaim/mr-okay -> mr-okay)
          REPO_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2)
          # Eğer repository adı username.github.io formatında ise basePath kullanma
          if [[ "$REPO_NAME" == *.github.io ]]; then
            echo "Repository is *.github.io format, basePath will be empty"
            BASE_PATH=""
          else
            echo "Repository name: $REPO_NAME"
            BASE_PATH="/$REPO_NAME"
          fi
          echo "BASE_PATH=$BASE_PATH" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_BASE_PATH=$BASE_PATH" >> $GITHUB_ENV
          echo "BasePath set to: $BASE_PATH"

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: ${{ env.NEXT_PUBLIC_BASE_PATH }}

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
